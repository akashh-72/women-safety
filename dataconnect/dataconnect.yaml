specVersion: "v1"
serviceId: "webadmin"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "webadmin-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
